defaults:
  - telegram: default
  - ollama: default
  - model: default
  - rag: default
  - cache: default
  - rate_limiter: default
  - validation: default
  - _self_

hydra:
  run:
    dir: logs/hydra/${now:%Y-%m-%d}/${now:%H-%M-%S}